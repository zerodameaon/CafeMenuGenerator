# Python
venv/
__pycache__/
*.pyc

# PyInstaller build output (regenerated by build_app.sh)
build/
dist/

# Packaged app zip (321MB — over GitHub's 100MB per-file limit; build
# locally with build_app.sh instead of committing it)
*.zip

# macOS
.DS_Store
